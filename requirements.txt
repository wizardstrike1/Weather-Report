# Core runtime dependencies for CTF Copilot
PySide6>=6.6
playwright>=1.44
anthropic>=0.40
pydantic>=2.6
loguru>=0.7
python-dotenv>=1.0
rich>=13.7

# Optional analysis extras (recommended)
scapy>=2.5
pillow>=10.0
numpy>=1.24    # spectrogram / LSB-audio / tone-envelope (audio CTFs)

# CTF solving extras — used by agent-authored file.write+python scripts.
# z3/sympy ship clean Windows wheels so they're safe to auto-install.
z3-solver>=4.12
sympy>=1.12
# pwntools / angr / sage are intentionally NOT listed: pwntools' build is
# fragile on Windows and would break `pip install -r requirements.txt`.
# Install them yourself where you actually run pwn/rev (Linux/WSL):
#   pipx install pwntools ; pipx install angr ; apt install sagemath
# The registry/agent degrade gracefully when they're absent.

# Dev / test
pytest>=8.0
pytest-asyncio>=0.23
