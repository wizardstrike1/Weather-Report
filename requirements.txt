# Core runtime dependencies for CTF Copilot
PySide6>=6.6
playwright>=1.44
anthropic>=0.40
pydantic>=2.6
loguru>=0.7
python-dotenv>=1.0
rich>=13.7

# Optional analysis extras (recommended)
scapy>=2.5
pillow>=10.0
numpy>=1.24    # spectrogram / LSB-audio / tone-envelope (audio CTFs)

# CTF solving extras — used by agent-authored file.write+python scripts
# (pwntools tubes/ROP, z3 & sympy constraint solving). angr/sage are heavy
# and intentionally NOT auto-installed; enable "Maximum tools" + install them
# yourself (pipx install angr; apt install sagemath).
pwntools>=4.11
z3-solver>=4.12
sympy>=1.12

# Dev / test
pytest>=8.0
pytest-asyncio>=0.23
